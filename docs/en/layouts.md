# Layouts

Use layout primitives to create varied rhythm and asymmetry. Current patterns include container, section, stack, grid, cluster, split, frame, flow, offset, layer, ma, sidebar, cover, reel, switcher, bleed, and masonry.
