# Getting Started

Install the package, import `dist/iroha.css`, and compose pages with `wa-*` classes. Use `data-wa-theme`, `data-wa-density`, and `data-wa-contrast` on a parent element to tune the whole page without new custom CSS.
