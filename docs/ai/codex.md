# Codex Usage

When editing a project that uses iroha-css, preserve the `wa-` prefix and `data-wa-theme` attribute. Use existing layout primitives before writing new CSS. Do not add raw hex colors unless extending primitive tokens.

