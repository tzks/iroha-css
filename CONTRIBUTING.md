# Contributing

Keep contributions aligned with the design system:

- Use `wa-` for public classes.
- Use `data-wa-theme` for themes.
- Do not add raw hex colors inside components.
- Prefer semantic tokens and component tokens.
- Avoid generic AI-looking SaaS layouts in examples.
- Do not copy external color datasets.

Run `npm run check` before opening a pull request.

